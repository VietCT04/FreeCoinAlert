# Shared environment variables belong here only when consumed
# by more than one application or service.
#
# Component-specific variables belong in:
# apps/<component>/.env.example
# services/<component>/.env.example
#
# Never commit real credentials or secrets.
